# 🐦 Flappy Bird Clone (iGraphics)

A simple "Flappy Bird clone" built in C & C++ using the iGraphics graphics library.  
This project recreates the classic arcade gameplay with smooth animations, scoring, and collision detection.

---

## ✨ Features
- Classic Flappy Bird mechanics (jump, gravity, obstacles)
- Real‑time graphics using iGraphics
- Score tracking system
- Game over & Life support functionality
- Lightweight and easy to run on Windows/Linux with OpenGL support

---

## 🛠 Technologies Used
- **C & C++**
- **iGraphics library** (built on OpenGL/GLUT)
- **VS Code** (development environment)

---

## 🚀 Getting Started

### Prerequisites
- Install a C/C++ compiler:
  - Windows: MinGW/TDM‑GCC
  - Linux/macOS: gcc/clang
- Install OpenGL/GLUT libraries:
  - Windows: `glut32.dll` in project/system folder
  - Linux/macOS: `sudo apt-get install freeglut3-dev` or `brew install freeglut`
- Add `iGraphics.h` and `iGraphics.cpp` to your project
